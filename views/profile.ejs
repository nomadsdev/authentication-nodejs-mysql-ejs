<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>โปรไฟล์</title>
    <%- include('./Config/Import') %>
</head>
<body>

    <%- include('./App/Nav') %>
    
    <div class="flex justify-center px-5 pt-20">
        <div class="w-full max-w-lg">
            <div class="w-full p-5 rounded-md bg-white shadow border text-center">
                <p>ยินดีต้อนรับคุณ <span class="text-indigo-500"><%= username %></span></p>
                <p class="text-zinc-600 text-sm"><%= email %></p>
            </div>
        </div>
    </div>

    <div class="flex justify-center px-5 py-10">
        <div class="w-full max-w-lg">
            <div class="w-full p-5 bg-white rounded-md shadow border">
                <h1 class="text-center text-indigo-500 text-xl pb-3">เพิ่มรูปภาพสไลด์</h1>
                <form action="/uploadimageslider" method="POST">
                    <div>
                        <label for="imageUrl" class="text-indigo-500pb-2">ลิ้งรูปภาพ <span class="text-red-500">*</span></label>
                        <br>
                        <input type="text" id="imageUrl" name="imageUrl" placeholder="ลิ้งรูปภาพ" class="border px-2 rounded-md w-full">
                    </div>
                    <br>
                    <button type="submit" class="border border-indigo-600 text-indigo-500 px-5 py-1 text-sm rounded-md hover:bg-indigo-600 hover:text-white transition">
                        เพิ่มรูปภาพ
                    </button>
                </form>
            </div>
        </div>
    </div>

    <div class="flex justify-center px-5">
        <div class="w-full max-w-lg">
            <button onclick="openModal()" class="w-full py-5 rounded-md outline-1 outline-indigo-500 outline-dashed  text-indigo-500">
                เปลี่ยนรหัสผ่าน
            </button>
            <div id="myModal" class="modal hidden backdrop-blur-sm fixed inset-0 bg-gray-500 bg-opacity-75 flex justify-center items-center">
                <div class="bg-white w-full rounded-lg p-5 max-w-lg">
                    <% if (errorChange) { %>
                        <p class="text-red-500 bg-red-200 text-center rounded-md py-1 w-full my-2 border text-sm border-red-500"><%= errorChange %></p>
                    <% } %>
                    <form action="/change-password" method="post">
                        <div>
                            <label for="oldPassword">รหัสผ่านเดิม <span class="text-red-500">*</span></label>
                            <input type="password" id="oldPassword" name="oldPassword" class="block w-full border border-gray-300 rounded-md px-2 py-1">
                        </div>
                        <br>
                        <div>
                            <label for="newPassword">รหัสผ่านใหม่ <span class="text-red-500">*</span></label>
                            <input type="password" id="newPassword" name="newPassword" class="block w-full border border-gray-300 rounded-md px-2 py-1">
                        </div>
                        <br>
                        <div>
                            <label for="confirmPassword">ยืนยันรหัสผ่านใหม่ <span class="text-red-500">*</span></label>
                            <input type="password" id="confirmPassword" name="confirmPassword" class="block w-full border rounded-md px-2 py-1">
                        </div>
                        <br>
                        <button type="submit" class="block w-full border border-indigo-500 text-indigo-500 rounded-md py-1 hover:bg-indigo-500 hover:text-white transition">ยืนยันเปลี่ยนรหัส</button>
                    </form>
                    <button onclick="closeModal()" class="block w-full border border-indigo-500 text-indigo-500 rounded-md py-1 hover:bg-indigo-500 hover:text-white transition mt-2">ยกเลิก</button>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        function openModal() {
            document.getElementById('myModal').classList.remove('hidden');
        }
    
        function closeModal() {
            document.getElementById('myModal').classList.add('hidden');
        }
    </script>

</body>
</html>